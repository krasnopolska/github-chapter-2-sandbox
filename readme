Testing git pull
🧪 Git Practice Repository

Цей репозиторій створено для практики основних команд Git 🧠
Навички, які ти потренуєш:

- 📂 `git init` — створення репозиторію
- ➕ `git add` — додавання файлів
- 📝 `git commit` — збереження змін
- 🌿 `git branch` — створення гілок
- 🔀 `git checkout` — перемикання між гілками
- 🧬 `git merge` — злиття гілок
- ⬇️ `git pull` — отримання змін
- ⬆️ `git push` — відправка змін
test